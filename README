1. brew install glew
2. brew install glfw
3. brew install pkg-config
4. install soil (https://github.com/childhood/libSOIL)  modify make install dir :  opt -> usr  in INCLUDEDIR  and LIBDIR
5. ./run.sh
